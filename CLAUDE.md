# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new project (`barcode-scanner`) with no source code yet. The only configuration present is Claude Code settings with the Supabase MCP server enabled (`.claude/settings.local.json`).

## Tooling

- **Supabase MCP** is configured and available — use it for database operations, auth, and storage when building out the backend.
